# smooooooth-operation
A coooool project for COOOL (CS6004), that implements object sensitive pointer analysis to perform code optimizations

# Instructions for running code
```bash
javac testcases/Test1/Test.java

javac -cp .:<path_to_soot.jar> PA4.java

java -cp ".:<path_to_soot.jar>" PA4 Test1
```